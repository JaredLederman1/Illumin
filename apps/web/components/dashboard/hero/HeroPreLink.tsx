'use client'

import HeroShell from './HeroShell'

export default function HeroPreLink() {
  return (
    <HeroShell
      eyebrow="Start here"
      headline="Illumin is standing watch."
      subtitle="Link your first account so Illumin can flag exactly how much money you are leaving on the table."
      ctaLabel="Link an account"
      ctaHref="/dashboard/accounts"
    />
  )
}
